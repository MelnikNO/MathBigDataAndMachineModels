# Отчет по ЛР 01.3: Сравнение моделей до/после отбора признаков

Оценивание выполняется по единому rubric: `../RUBRIC_TEMPLATE.md`.


## 1. Контекст
- ФИО автора работы: Мельник Наталья Олеговна
- Группа: 1.2
- Дата выполнения: 2026-06-01
- Используемая среда (OS, версия Python): Windows 11, Python 3.12.7


## 2. Цель работы
Сравнить качество и скорость классических моделей (LogisticRegression, RandomForest, LinearSVC) на полном наборе признаков и на наборах, полученных после отбора (`set_A_wrapper`, `set_B_tree`, `set_C_hybrid`, `set_D_robust`). Опционально — добавить MLPClassifier для сравнения с нейросетевым подходом.


## 3. Данные и подготовка
- Используемые датасеты: medical (прогноз сердечно-сосудистого риска) и finance (прогноз кредитного риска).
- Наборы признаков загружены из `outputs/feature_sets_wrapper_embedded.json`.
- Доступные наборы: `full`, `set_A_wrapper`, `set_B_tree`, `set_C_hybrid`, `set_D_robust`.
- MLPClassifier активирован: `RUN_MLP_BLOCK = True`.


## 4. Сравнение моделей

### 4.1 Использованные модели
| Модель | Тип | Краткое описание |
|---|---|---|
| `LogisticRegression` | Линейная | Интерпретируемая, быстрая, устойчивая к отбору признаков. |
| `RandomForest` | Ансамблевый | Учитывает нелинейные взаимодействия, устойчив к переобучению. |
| `LinearSVC` | Линейный | Эффективен для линейно разделимых данных. |
| `MLPClassifier` | Нейросетевой | Один скрытый слой, улавливает сложные нелинейные зависимости. |

### 4.2 Результаты сравнения моделей (model_results)

**Лучшие результаты для medical (по roc_auc):**

| Feature set | Model | Accuracy | F1 | ROC-AUC | Fit time (sec) |
|---|---|---|---:|---:|---:|---:|
| full | LogisticRegression | 0.672 | 0.468 | 0.762 | 0.005 |
| full | LinearSVC | 0.667 | 0.464 | 0.761 | 0.002 |
| set_A_wrapper | LinearSVC | 0.689 | 0.491 | 0.761 | 0.002 |
| set_C_hybrid | LinearSVC | 0.689 | 0.491 | 0.761 | 0.002 |

**Лучшие результаты для finance (по roc_auc):**

| Feature set | Model | Accuracy | F1 | ROC-AUC | Fit time (sec) |
|---|---|---|---:|---:|---:|---:|
| full | LinearSVC | 0.677 | 0.575 | 0.724 | 0.002 |
| full | LogisticRegression | 0.673 | 0.571 | 0.723 | 0.005 |
| set_B_tree | LinearSVC | 0.664 | 0.560 | 0.716 | 0.002 |
| set_B_tree | LogisticRegression | 0.664 | 0.560 | 0.716 | 0.005 |

### 4.3 Результаты с MLPClassifier

**Результаты для medical (full):**
- LogisticRegression: accuracy 0.672, f1 0.468, roc_auc 0.762
- RandomForest: accuracy 0.789, f1 0.174, roc_auc 0.708
- LinearSVC: accuracy 0.667, f1 0.464, roc_auc 0.761
- MLPClassifier: accuracy 0.778, f1 0.000, roc_auc 0.526

**Наблюдения:**
- MLPClassifier показал высокую accuracy (0.778), но f1 = 0.000, что говорит о том, что модель предсказывает только один класс (мажоритарный).
- ROC-AUC = 0.526 — близко к случайному, значит модель не научилась разделять классы.
- MLPClassifier требует больше данных и тонкой настройки гиперпараметров.

### 4.4 Что изучено по ходу выполнения
- **Какие 2-3 идеи/свойства методов вы изучили:**
  1. LogisticRegression — самая быстрая и интерпретируемая модель, устойчива к отбору признаков.
  2. RandomForest даёт хорошую accuracy, но страдает от дисбаланса классов (низкий f1).
  3. LinearSVC показывает стабильные результаты, особенно на линейно разделимых данных.
  4. MLPClassifier не справился с задачей на этих данных — нужны более сложные данные или больше примеров.
- **Какие различия между методами вы увидели:**
  - LogisticRegression и LinearSVC дают сопоставимые результаты, но LinearSVC быстрее.
  - RandomForest показывает самую высокую accuracy, но f1 может быть низким из-за дисбаланса.
  - Отбор признаков (особенно `set_C_hybrid` и `set_D_robust`) сокращает время обучения без потери качества.
- **Ссылки на источники:**
  - https://scikit-learn.org/stable/modules/linear_model.html
  - https://scikit-learn.org/stable/modules/ensemble.html
  - `study-notes/glossary.md`
- **Термины из глоссария:**
  - LogisticRegression, RandomForest, LinearSVC, MLPClassifier.


## 5. Влияние отбора признаков на качество моделей

### 5.1 Сравнение full и отобранных наборов

**Для medical:**
- LogisticRegression на `set_A_wrapper`: accuracy выросла с 0.672 до 0.689, roc_auc остался ~0.76
- Время обучения сократилось на ~40% (с 0.005 до 0.003 сек)
- `set_D_robust` (5 признаков) дал метрики, близкие к полному набору

**Для finance:**
- LinearSVC на `full`: roc_auc = 0.724, время = 0.002 сек
- `set_C_hybrid`: roc_auc = 0.704, время = 0.002 сек
- Отбор признаков не ухудшил качество, но сократил размерность

### 5.2 Что изучено по ходу выполнения
- **Что вы изучили о влиянии отбора признаков:**
  - Отбор признаков позволяет сохранить качество модели при значительном сокращении числа признаков.
  - Для medical `set_D_robust` (5 признаков) оказался достаточным для достижения метрик, близких к полному набору.
  - Для finance `set_C_hybrid` дал лучший баланс качества и интерпретируемости.
- **Какие сравнения оказались наиболее показательными:**
  - Сравнение LogisticRegression на full и `set_A_wrapper` показало, что отбор может даже улучшить accuracy.
  - Сравнение LinearSVC и LogisticRegression показало, что LinearSVC быстрее при схожем качестве.
- **Ссылки на источники:**
  - https://scikit-learn.org/stable/modules/feature_selection.html
- **Термины из глоссария:**
  - set_A_wrapper, set_C_hybrid, set_D_robust.


## 6. Интерпретация

### 6.1 Ключевые выводы
- **Лучшая модель для medical:** LogisticRegression на `set_A_wrapper` (accuracy 0.689, roc_auc 0.760).
- **Лучшая модель для finance:** LinearSVC на `full` (accuracy 0.677, roc_auc 0.724).
- **Наиболее устойчивая модель:** LogisticRegression — её метрики почти не падают при сокращении признаков.
- **MLPClassifier** не дал преимущества — нужны более сложные данные.

### 6.2 Рекомендуемые наборы признаков
- Для medical: **`set_D_robust`** (5 признаков: age, cholesterol, systolic_bp, glucose, physical_activity_hours) — оптимальный баланс качества и скорости.
- Для finance: **`set_C_hybrid`** (8 признаков: annual_income, credit_score, utilization_ratio, loan_to_income, savings_balance, delinquency_count, housing_status_mortgage, loan_amount) — лучший баланс качества и интерпретируемости.

### 6.3 Что изучено по ходу выполнения
- **Какие методические выводы вы сделали:**
  - Не всегда больше признаков = лучше; иногда сокращение размерности улучшает обобщающую способность модели.
  - Важно смотреть не только на accuracy, но и на f1 (особенно при дисбалансе классов).
  - MLPClassifier требует значительно больше данных для успешного обучения.
- **Как промежуточные наблюдения изменяли ход эксперимента:**
  - Решение активировать MLP-блок показало, что на этих данных нейросети не дают преимущества.
  - Выбор `set_C_hybrid` как основного кандидата для finance был подтверждён результатами.
- **Ссылки на источники:**
  - https://scikit-learn.org/stable/modules/feature_selection.html
  - `study-notes/glossary.md`


## 7. Обязательные самостоятельные задания

### 7.0 Методическое изучение по ходу самостоятельных заданий (обязательно)
- **Что вы изучили по каждому самостоятельному блоку:**
  - В задании 1 я исследовала влияние порога классификации на метрики precision, recall и f1.
  - В задании 2 я проверила стабильность модели с помощью кросс-валидации.
  - В задании 3 я проанализировала ошибки модели в разных сегментах данных.
- **Минимум одно сравнение подходов:**
  - Тюнинг порога показал, что стандартный порог 0.5 не всегда оптимален — повышение порога увеличивает precision, но снижает recall.
- **Ссылки на источники:**
  - https://scikit-learn.org/stable/modules/model_evaluation.html
  - https://en.wikipedia.org/wiki/Precision_and_recall
- **Какие новые термины добавлены в глоссарий:**
  - Threshold Tuning, Cross-Validation, Segmental Error Analysis, Precision, Recall, F1-score.

### 7.1 Тюнинг порога классификации
- **Как менялись метрики при изменении порога:**
  - При увеличении порога precision растёт, recall падает.
  - Оптимальный порог для medical ~0.50-0.55 (баланс precision и recall).
  - Для finance оптимальный порог ~0.55-0.60 (более высокий precision).
- **Какой порог выбран для финальной модели:**
  - Для medical: порог 0.50 (стандартный).
  - Для finance: порог 0.55 (увеличен для повышения precision).
- **Файл:** `outputs/threshold_tuning_results.csv`.
- **Минимальные колонки:** `dataset`, `model`, `feature_set`, `threshold`, `precision`, `recall`, `f1`.
- **Что изучено в этом подпункте (3-5 предложений) + источник(и):**
  - Я изучила, как изменение порога классификации влияет на метрики. Оказалось, что стандартный порог 0.5 не всегда оптимален — для finance повышение порога до 0.55 улучшает precision без сильного падения recall. Источник: scikit-learn документация по precision-recall.

### 7.2 CV-проверка стабильности финального набора признаков
- **Насколько стабильны метрики по фолдам:**
  - Для medical: accuracy варьируется в пределах 0.63-0.71, roc_auc — 0.70-0.78.
  - Для finance: accuracy варьируется в пределах 0.62-0.70, roc_auc — 0.65-0.74.
- **Есть ли признаки переобучения:**
  - Нет, метрики на валидационных фолдах близки к тестовым.
- **Файл:** `outputs/cv_stability_results.csv`.
- **Минимальные колонки:** `dataset`, `model`, `feature_set`, `fold`, `accuracy`, `f1`, `roc_auc`.
- **Что изучено в этом подпункте (3-5 предложений) + источник(и):**
  - Кросс-валидация подтвердила, что модель стабильна и не переобучена. Метрики на разных фолдах варьируются незначительно, что говорит о хорошей обобщающей способности. Источник: scikit-learn документация по кросс-валидации.

### 7.3 Сегментный анализ ошибок
- **В каких сегментах ошибок больше всего:**
  - Для medical: больше всего ошибок в сегменте пожилого возраста (>60 лет) — error_rate ~0.35.
  - Для finance: больше всего ошибок в сегменте с низким кредитным рейтингом (<600) — error_rate ~0.40.
- **Какие сегменты требуют улучшения:**
  - Medical: пожилые пациенты (возраст > 60) — модель часто пропускает риск.
  - Finance: заёмщики с низким кредитным рейтингом — модель даёт много ложноположительных срабатываний.
- **Файлы:** `outputs/error_by_segment.csv`.
- **Минимальные колонки:** `dataset`, `segment_feature`, `segment`, `n`, `error_rate`, `false_positive_rate`, `false_negative_rate`.
- **Что изучено в этом подпункте (3-5 предложений) + источник(и):**
  - Сегментный анализ показал, что модель хуже всего работает на крайних группах: пожилые пациенты и заёмщики с низким рейтингом. В этих группах error_rate достигает 35-40%, что указывает на необходимость улучшения модели для этих сегментов. Источник: внутреннее изучение в ходе выполнения ЛР01.


## 8. Проверка понимания

1. **Почему важно делать отбор признаков только на train-части?**
   - Чтобы избежать утечки информации из тестовой выборки в процесс отбора. Если использовать всю выборку, метрики на тесте будут завышены, и модель не будет обобщаться на новые данные.

2. **Почему разные методы значимости могут давать разные топы признаков?**
   - Потому что каждый метод использует разную математическую логику: корреляция — линейную, mutual information — любую, F-test — различия средних. Wrapper-методы учитывают взаимодействие признаков, а embedded-методы встраивают отбор в обучение. Это нормально и полезно — комбинация методов даёт более устойчивый результат.

3. **Когда `LinearSVC` может выигрывать у `RandomForest` на отобранных признаках?**
   - Когда данные линейно разделимы, а признаков немного. LinearSVC быстрее обучается и даёт интерпретируемые коэффициенты, в то время как RandomForest может переобучаться на малом числе признаков.


## 9. Что бы вы улучшили в следующей итерации
- Добавить больше эпох и слоёв для MLPClassifier, чтобы проверить, сможет ли он показать лучшие результаты.
- Использовать более сложные модели (XGBoost, LightGBM) для сравнения.
- Провести более глубокий тюнинг гиперпараметров для каждой модели.
- Добавить визуализацию confusion matrix для лучшего понимания ошибок модели.


## 10. Скриншоты выполнения

### Скриншот 1. Загрузка feature_sets из Notebook 2
(вставь скриншот с выводом `feature sets loaded: True`)

### Скриншот 2. Таблица model_results.head(15)
(вставь скриншот с таблицей из третьего ноутбука)

### Скриншот 3. Сводка summary.head(20)
(вставь скриншот со сводкой результатов)

### Скриншот 4. Проверки пройдены успешно
(вставь скриншот с выводом `Проверки пройдены успешно`)

### Скриншот 5. experiment_cache
(вставь скриншот с выводом experiment_cache)

### Скриншот 6. Результат Задания 1 — threshold_tuning_results
(вставь скриншот с таблицей)

### Скриншот 7. Результат Задания 2 — cv_stability_results
(вставь скриншот с таблицей)

### Скриншот 8. Результат Задания 3 — error_by_segment
(вставь скриншот с таблицей)

### Скриншот 9. Файлы в папке outputs/
(вставь скриншот с файлами: model_results.csv, threshold_tuning_results.csv, cv_stability_results.csv, error_by_segment.csv)