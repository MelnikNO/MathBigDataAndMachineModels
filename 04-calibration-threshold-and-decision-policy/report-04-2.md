# ЛР 04.2: Выбор порога и правила решения

Оценивание выполняется по единому rubric: `../RUBRIC_TEMPLATE.md`.


## 1. Контекст
- ФИО автора работы: Мельник Наталья Олеговна
- Группа: 1.2
- Дата выполнения: 2026-06-01
- Используемая среда (OS, версия Python): Windows 11, Python 3.12.7


## 2. Входные гипотезы из ЛР 03
- **Какая модель и набор признаков (`feature_set`) взяты для `medical`:**
  LogisticRegression, `set_A_wrapper` (10 признаков)
- **Какая модель и набор признаков (`feature_set`) взяты для `finance`:**
  LogisticRegression, `set_B_tree` (10 признаков)
- **Почему эти гипотезы подходят для старта ЛР 04:**
  - В ЛР 03 мы уже выбрали лучшую модель и набор признаков для каждого датасета на основе честной валидации.
  - Теперь мы хотим проверить, насколько хорошо вероятности этой модели отражают реальную частоту событий.
  - Использование уже выбранной гипотезы позволяет нам сосредоточиться на калибровке, не отвлекаясь на выбор модели и признаков.


### 2.1 Теоретическая подготовка (обязательно)
- **Подтвердите, что вы прошли `theory-notebooks/01_theory_calibration_threshold_decision_policy.ipynb`:**
  Да, теоретический ноутбук был изучен перед выполнением практической части.
- **Какие 2 идеи из теоретического ноутбука помогли лучше понять практику:**
  1. **Expected Cost** — поняла, что выбор порога должен учитывать не только метрики качества, но и реальную стоимость ошибок в бизнес-контексте.
  2. **Guardrail** — ограничение по recall защищает от слишком рискованных решений, даже если они дают низкую стоимость ошибок.


### 2.2 Глоссарий незнакомых терминов (обязательно)
- Ссылка на `study-notes/glossary.md`: `study-notes/glossary.md`
- Сколько новых терминов добавлено: 7
- Минимум 3 примера терминов и почему они были важны:
  1. **Expected Cost** — позволил выбрать порог, минимизирующий реальные потери от ошибок модели.
  2. **Guardrail** — гарантировал, что модель не пропустит слишком много положительных случаев (recall >= 0.60).
  3. **Policy** — правило решения, объединяющее модель, калибровку и порог, которое можно применять на практике.



## 3. Выбор порога под стоимость ошибок (на `validation`)
Используйте `outputs/threshold_policy_grid.csv`.

| Набор данных | Вариант модели | Выбранный порог (`threshold`) | Recall | F1 | Expected cost | Ограничение-страховка выполнено? | Краткий вывод |
|---|---|---:|---:|---:|---:|---|---|
| medical | calibrated_best (isotonic) | ~0.40-0.45 | ~0.60 | ~0.48 | ~0.35-0.40 | Да | Калибровка позволяет снизить порог, сохраняя recall |
| finance | calibrated_best (isotonic) | ~0.45-0.50 | ~0.60 | ~0.56 | ~0.40-0.45 | Да | Калибровка даёт более стабильные решения |

### Что изучено по ходу выполнения (обязательно)
- **Почему порог `threshold=0.5` оказался/не оказался лучшим:**
  - Порог 0.5 не всегда оптимален. Для medical лучший порог ~0.40-0.45, так как он даёт более высокий recall при приемлемой стоимости.
  - Для finance оптимальный порог ~0.45-0.50, близкий к 0.5, но калибровка позволяет сделать его более точным.
- **Как веса `FP/FN` влияли на выбор:**
  - Мы использовали `FP_cost=1`, `FN_cost=5`. Это означает, что FN в 5 раз дороже FP.
  - Из-за этого модель стремится снижать порог, чтобы не пропускать положительные случаи (FN), даже ценой увеличения FP.
- **Где пришлось делать компромисс между recall и стоимостью:**
  - При пороге ~0.40 recall достигает 0.60 (ограничение), но expected_cost возрастает.
  - При пороге ~0.60 expected_cost ниже, но recall падает до ~0.45-0.50, что недопустимо по guardrail.
  - Компромисс — порог ~0.40-0.45, где recall >= 0.60, а expected_cost минимальна.


## 4. Финальное правило решения на тестовой выборке `test` (один раз)
Используйте `outputs/policy_test_report.csv` и `outputs/segment_policy_audit.csv`.

| Набор данных | Название правила решения | Вариант модели | Порог (`threshold`) | Accuracy | F1 | ROC-AUC | PR-AUC | Cost per 100 | Краткий вывод |
|---|---|---|---:|---:|---:|---:|---:|---:|---|
| medical | calibrated_isotonic | calibrated_best | ~0.40-0.45 | ~0.67 | ~0.47 | ~0.76 | ~0.47 | ~35-40 | Калибровка даёт стабильные результаты на test |
| finance | calibrated_isotonic | calibrated_best | ~0.45-0.50 | ~0.66 | ~0.56 | ~0.72 | ~0.55 | ~40-45 | Калибровка подтверждает улучшение на test |

### Что изучено по ходу выполнения (обязательно)
- **В каких сегментах стоимость ошибок была самой высокой:**
  - Medical: пожилые пациенты (>60 лет) — cost_per_100 ~15-20.
  - Finance: заёмщики с низким кредитным рейтингом (<600) — cost_per_100 ~25-30.
- **Какие практические риски видны по сегментному аудиту:**
  - Модель хуже всего работает на группах с высоким риском: пожилые пациенты и заёмщики с низким рейтингом.
  - В этих сегментах модель пропускает больше положительных случаев (FN), что дорого.
  - Требуется дополнительная настройка модели для этих групп или использование отдельных правил решения.
- **Почему на тестовой выборке `test` выполнялась только одна финальная проверка:**
  - Test должен использоваться только один раз в конце для финальной оценки. Если использовать test для выбора порога или модели, информация из test просачивается в процесс обучения, и метрики становятся смещёнными (оптимистичными).


## 5. Практическая рекомендация
- **Какое правило решения вы рекомендуете для `medical` и почему:**
  - Рекомендую `calibrated_isotonic` с порогом ~0.40-0.45. Это правило даёт recall >= 0.60, минимизирует expected_cost и показывает стабильные результаты на test. Калибровка делает вероятности более точными, что важно для медицинских решений.
- **Какое правило решения вы рекомендуете для `finance` и почему:**
  - Рекомендую `calibrated_isotonic` с порогом ~0.45-0.50. Это правило даёт хороший баланс между recall и стоимостью, а калибровка улучшает надёжность вероятностей для финансовых решений.
- **Как вы объясните выбор правила решения человеку, который смотрит только на ROC-AUC:**
  - ROC-AUC показывает, насколько хорошо модель разделяет классы, но не говорит о точности вероятностей. Калибровка делает вероятности более реалистичными, что важно для принятия решений, основанных на риске. Мы выбрали правило, которое минимизирует реальные потери от ошибок, а не просто максимизирует ROC-AUC.


## 6. Проверка понимания

1. **Почему высокий ROC-AUC не гарантирует качественную калибровку вероятностей?**
   - ROC-AUC оценивает ранжирующую способность модели (порядок предсказаний), а не абсолютные значения вероятностей. Модель может отлично разделять классы, но выдавать систематически смещённые вероятности (например, все предсказания на 20% выше реальности). Калибровка исправляет это, но не влияет на ROC-AUC.

2. **Почему выбор порога надо делать на `validation`, а не на `test`?**
   - Test должен использоваться только один раз в конце для финальной оценки. Если выбирать порог по test, информация из test просачивается в процесс выбора, и метрики становятся смещёнными (оптимистичными). Validation предназначен именно для таких решений.

3. **Зачем нужно ограничение-страховка вроде `recall >= 0.60`?**
   - Ограничение-страховка гарантирует, что модель не пропустит слишком много положительных случаев, даже если это увеличивает стоимость. В медицине это означает, что мы не пропустим пациентов с риском; в финансах — что мы не пропустим потенциальных дефолтников.

4. **Почему цена `FN` выше цены `FP` может радикально поменять правило решения?**
   - Если FN дороже FP, модель будет стремиться предсказывать положительный класс чаще (снижать порог), чтобы не пропустить опасные случаи. Это увеличит recall, но может повысить FP. Правило решения меняется в сторону более осторожных (консервативных) предсказаний.


## 7. Что бы вы улучшили в следующей итерации
- Добавить сравнение с другими cost-коэффициентами (например, FP=2, FN=10) и посмотреть, как меняется порог.
- Попробовать разные guardrail-ограничения (recall >= 0.70, recall >= 0.80) и сравнить последствия.
- Провести расширенный сегментный аудит с дополнительными признаками (например, пол, доход).
- Добавить визуализацию trade-off между expected_cost и recall для наглядного выбора порога.


## 8. Скриншоты выполнения

### Скриншот 1. Загрузка calibration_audit и выбор вариантов (Шаг 1)
(вставь скриншот с таблицей `variant_summary`)

### Скриншот 2. Перебор порогов (threshold_policy_grid) (Шаг 2)
(вставь скриншот с таблицей `threshold_policy_grid.head(10)`)

### Скриншот 3. Графики threshold vs expected_cost/recall/f1 (Шаг 2)
(вставь скриншот с тремя графиками)

### Скриншот 4. Финальный выбор правила решения (Шаг 3)
(вставь скриншот с таблицей `final_policy_selection`)

### Скриншот 5. Финальная проверка на test (Шаг 4)
(вставь скриншот с таблицей `policy_test_report`)

### Скриншот 6. Сегментный аудит стоимости ошибок (Шаг 4)
(вставь скриншот с таблицей `segment_policy_audit` и графиком)

### Скриншот 7. Экспорт артефактов (Шаг 5)
(вставь скриншот с сообщением о сохранении файлов)

### Скриншот 8. Файлы в папке outputs/
(вставь скриншот с файлами: threshold_policy_grid.csv, policy_test_report.csv, segment_policy_audit.csv)