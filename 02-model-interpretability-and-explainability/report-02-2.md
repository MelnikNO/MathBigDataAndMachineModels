# Отчет по ЛР 02.2: Локальный разбор ошибок и сегментов

Оценивание выполняется по единому rubric: `../RUBRIC_TEMPLATE.md`.


## 1. Контекст
- ФИО автора работы: Мельник Наталья Олеговна
- Группа: 1.2
- Дата выполнения: 2026-06-01
- Используемая среда (OS, версия Python): Windows 11, Python 3.12.7


## 2. Входные данные и предпосылки
- **Какая лучшая пара `model + feature_set` из ЛР 01 выбрана для `medical`:**
  `LinearSVC` + `set_A_wrapper` (roc_auc = 0.7605, f1 = 0.4909, accuracy = 0.6889)
- **Какая лучшая пара `model + feature_set` из ЛР 01 выбрана для `finance`:**
  `LinearSVC` + `set_B_tree` (roc_auc = 0.7162, f1 = 0.5595, accuracy = 0.6636)
- **Почему выбраны именно эти пары:**
  - Это лучшие комбинации по roc_auc из ЛР 01 для каждого датасета.
  - LinearSVC показал стабильные результаты и хорошо интерпретируется через коэффициенты и perturbation-анализ.


## 2.1 Глоссарий незнакомых терминов (обязательно)
- Ссылка на `study-notes/glossary.md`: `study-notes/glossary.md`
- Сколько новых терминов добавлено по ходу всей ЛР: 4
- Минимум 3 примера терминов и почему они были важны:
  1. **Perturbation-анализ** — показал, как изменение значений признаков влияет на предсказание в конкретном случае.
  2. **False Positive / False Negative** — позволил разделить ошибки модели и понять их природу.
  3. **Decision Memo** — помог структурировать практические рекомендации по выбору модели.



## 3. Локальный разбор ошибок

### 3.1 Анализ false positive и false negative

**Для датасета `medical`:**

| Тип ошибки | Часто встречающиеся признаки | Средняя важность (importance_value) | Комментарий |
|---|---|---|---|
| false_positive | age, cholesterol, systolic_bp, smoking_status | num__physical_activity_hours (0.329), num__systolic_bp (0.325), num__age (0.309) | Модель переоценивает возраст и холестерин. У пациентов среднего возраста с повышенным холестерином, но без других факторов риска, модель ошибается. |
| false_negative | age, cholesterol, systolic_bp, smoking_status | num__cholesterol (0.338), num__systolic_bp (0.312), num__age (0.304) | Модель недооценивает влияние физической активности и стресса. У пациентов с низкой активностью и высоким стрессом модель не распознаёт риск. |

**Выводы для medical:**
- False positive связаны с переоценкой age, cholesterol, systolic_bp.
- False negative связаны с недооценкой physical_activity_hours и stress_level.
- Модель систематически ошибается на пожилых пациентах и пациентах с повышенным холестерином.

**Для датасета `finance`:**

| Тип ошибки | Часто встречающиеся признаки | Средняя важность (importance_value) | Комментарий |
|---|---|---|---|
| false_positive | annual_income, housing_status, credit_score, previous_default_no | num__loan_to_income (0.467), num__annual_income (0.305), cat__previous_default_no (0.225) | Модель переоценивает долговую нагрузку. У заёмщиков с высоким loan_to_income, но хорошей кредитной историей модель ошибается. |
| false_negative | credit_score, annual_income, previous_default_no, utilization_ratio | num__credit_score (0.258), cat__previous_default_no (0.225), num__annual_income (0.199) | Модель недооценивает кредитный рейтинг и просрочки. У заёмщиков с низким рейтингом и просрочками модель не распознаёт риск дефолта. |

**Выводы для finance:**
- False positive связаны с переоценкой loan_to_income.
- False negative связаны с недооценкой credit_score и delinquency_count.
- Модель лучше объясняет false positive, чем false negative — для false negative признаки менее выражены.

### 3.2 Что изучено по ходу выполнения (обязательно)
- **Какие методические выводы вы сделали во время локального анализа:**
  - Локальный анализ показал, что одни и те же признаки (age, cholesterol) могут быть важны как для false positive, так и для false negative, но в разных контекстах.
  - Для false positive признаки часто имеют экстремальные значения (высокий возраст, высокий холестерин).
  - Для false negative признаки часто имеют средние значения, которые модель не считает критическими.
- **Как сегментный анализ дополнил локальные объяснения:**
  - Сегментный анализ показал, что ошибки сконцентрированы в определённых группах: пожилые пациенты (medical) и заёмщики с низким кредитным рейтингом (finance).
  - Локальные объяснения показали, какие именно признаки в этих группах приводят к ошибкам.
  - Комбинация подходов даёт полную картину: где модель ошибается (сегменты) и почему (локальные объяснения).
- **Ссылки на источники:**
  - https://christophm.github.io/interpretable-ml-book/
- **Какие термины из `study-notes/glossary.md` использовали:**
  - Perturbation-анализ, False Positive, False Negative, Decision Memo.


## 4. Практическая рекомендация

- **Какая модель лучше объяснима для `medical` и почему:**
  - **LogisticRegression** лучше объяснима для medical, так как её коэффициенты показывают направление влияния каждого признака. Это важно для медицинских задач, где врачам нужно понимать, какие факторы повышают риск.
- **Какая модель лучше объяснима для `finance` и почему:**
  - **LogisticRegression** также лучше объяснима для finance — коэффициенты показывают, что loan_to_income увеличивает риск, а credit_score снижает. Это согласуется с финансовой логикой.
- **Где вы бы выбрали более простую модель даже при близких метриках:**
  - LogisticRegression предпочтительнее RandomForest и LinearSVC, когда важна интерпретируемость. На этих данных LR показывает roc_auc, близкий к LinearSVC, но даёт понятные и простые объяснения. В медицине и финансах интерпретируемость критична для принятия решений.


## 5. Обязательные самостоятельные задания (без образца в solutions)


### Локальные объяснения ошибок

**Какие ошибки оказались наиболее показательными:**
- Для medical: false positive у пациентов старше 60 лет с повышенным холестерином, но без других факторов риска. Модель переоценивает возраст и холестерин.
- Для finance: false positive у заёмщиков с высоким loan_to_income, но хорошей кредитной историей. Модель переоценивает долговую нагрузку.

**Какие признаки чаще всего объясняют ошибки через perturbation-анализ:**
- Medical: age, cholesterol, systolic_bp (для false positive); physical_activity_hours, stress_level (для false negative).
- Finance: loan_to_income, annual_income (для false positive); credit_score, delinquency_count (для false negative).

**Файл:** `outputs/error_case_explanations.csv`.


## 6. Проверка понимания

1. **Почему коэффициенты логистической регрессии нельзя интерпретировать без учета препроцессинга?**
   - Коэффициенты LR зависят от масштаба признаков. Если признаки не стандартизированы, коэффициенты несопоставимы. В данном ноутбуке препроцессинг включает стандартизацию числовых признаков, поэтому коэффициенты можно сравнивать.

2. **Почему permutation importance может расходиться с native importance?**
   - Native importance для RandomForest может быть смещена в пользу признаков с большим числом уникальных значений. Permutation importance оценивает реальное влияние на метрику, поэтому она более надёжна.

3. **Когда partial dependence стоит трактовать осторожно?**
   - Когда признак сильно коррелирует с другими признаками. На краях распределения, где мало данных, кривые могут быть нестабильными. Также partial dependence не показывает причинность — только ассоциацию.

4. **Почему локальное объяснение ошибки не равно причинному объяснению?**
   - Локальное объяснение показывает, какие признаки были наиболее важны для конкретного предсказания модели. Это не означает, что эти признаки «вызвали» ошибку — модель могла ошибиться из-за шума в данных, отсутствия важных признаков или неправильной калибровки. Причинность требует контролируемых экспериментов.


## 7. Что бы вы улучшили в следующей итерации
- Добавить больше методов локальной интерпретации (например, SHAP или LIME) для сравнения с perturbation-анализом.
- Проверить устойчивость локальных объяснений при разных random_state.
- Визуализировать распределение важности признаков для false positive и false negative.
- Сравнить локальные объяснения для разных моделей (LR, RF, LinearSVC) на одних и тех же ошибочных кейсах.


## 8. Скриншоты выполнения

### Скриншот 1. Выбор лучшей пары model + feature_set (selection_summary)
(вставь скриншот с таблицей selection_summary)

### Скриншот 2. Локальные объяснения ошибок (error_case_explanations)
(вставь скриншот с таблицей error_case_explanations.head(20))

### Скриншот 3. Сегментный анализ ошибок (segment_error_summary)
(вставь скриншот с таблицей segment_error_summary)

### Скриншот 4. Результат Задания 1 — сравнение false positive и false negative
(вставь скриншот с выводом анализа)

### Скриншот 5. Результат Задания 2 — проверка сегментов риска
(вставь скриншот с таблицами топ-сегментов)

### Скриншот 6. Результат Задания 3 — экспорт и decision memo
(вставь скриншот с сообщением о сохранении файла и decision memo)

### Скриншот 7. Файлы в папке outputs/
(вставь скриншот с файлами: error_case_explanations.csv, а также файлами из первого ноутбука)